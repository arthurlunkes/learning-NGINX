# Provisionamento automatico: o Grafana ja sobe com o Prometheus conectado,
# sem precisar clicar em nada.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
